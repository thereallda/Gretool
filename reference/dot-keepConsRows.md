# Keep Consistent Rows in Object

Make sure the rows in `counts` slot and `assay` are consistent.

## Usage

``` r
.keepConsRows(object)
```

## Arguments

- object:

  object

## Value

Updated object
